import base from './base.mjs';

/**
 * React-Variante der ESLint-Config. Apps wie `@salon-os/web` nutzen diese.
 * react-specific plugins werden hier ergänzt, sobald wir die exakten Versionen festlegen.
 */
export default base;
